
  # Addressing Human Computer Interaction standards with an E-commerce Website Prototype 

  This is a code bundle for a Luxury E-commerce Website Prototype eveloped using various Figma tools.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  